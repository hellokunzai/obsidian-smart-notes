# 2026-08-13 工作日志

## 模型链接多配置重构（Vault Mind 插件）
- 需求：设置页「模型链接」支持多个链接、每个链接多个模型；原字段移入弹窗，列表加搜索/编辑/删除/设为默认。
- 方案（已与用户确认原型；运行时选「列表加默认标记」，默认链接取第一个模型）。
- 改动文件：
  - `src/settings.ts`：新增 `ModelLink` 接口、`genId()`、`modelLinks`/`defaultModelLinkId` 字段；重写 `renderProviderTab` 的模型链接分组 + 新增 `renderModelLinkList`。
  - `src/modelLinkModal.ts`（新）：添加/编辑弹窗，含链接名称唯一校验、多模型标签编辑器、API Key 显隐、测试连接。
  - `src/ai/provider.ts`：新增 `getActiveModelLink`/`createProviderFromLink` + NoopProvider 兜底；`createProvider` 改用默认链接。
  - `src/main.ts`：`loadSettings()` 增加旧 provider 配置迁移；导入 `genId`。
  - `src/i18n/locales/{zh,en}.json`：补充模型链接相关文案 + `error.noModelLink`。
  - `styles.css`：列表/弹窗样式。
  - 版本 0.9.3 → 0.10.0（manifest/package/versions 三处同步）。
- 验证：npm run build 通过（0 TS 错误）；安全扫描无 createContextualFragment/eval/new Function。
- 注意：未 git 提交（遵循「未经明确说 git 提交不得提交」死命令）。
