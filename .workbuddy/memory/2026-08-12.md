# 2026-08-12

## AI Note Agent 插件：知识库索引 + 显式文件/文件夹附加（v0.3.0 → v0.4.0）

按 obsidian-plugin-dev 阶段流程完成开发，已通过 tsc 构建、安全扫描、i18n 校验、版本同步。

### 需求
- AI 能看到知识库所有文件（仅路径索引，不读内容），但对话中默认不加载任何文件内容。
- 只有用户显式附加文件/文件夹，或在消息中点名某文件时，才读取其内容与上下文。
- 聊天框上方新增「附件栏 + 从知识库选择文件/文件夹」入口。

### 关键设计决策（用户确认）
- 仅显式附加：移除旧的「自动附带当前活跃笔记」逻辑（删 getNoteContext/getActiveNote）。
- 附件按会话独立保存（Session.attachments: {type:file|folder, path}[]）。
- 索引仅含 Markdown 文件。

### 改动文件
- src/utils/aiFolder.ts：Session 加 attachments 字段 + 迁移兼容。
- src/context/knowledge.ts（新增）：buildKnowledgeIndex（系统提示注入路径目录）、buildAttachmentContext（解析附件→递归读内容→拼接上下文块，单文件截断、消息点名匹配）。预留 skill/web 扩展点。
- src/view/chatView.ts：附件栏 UI + AttachmentPickerModal 选择器；发送调用附件上下文；buildSystem 注入索引；彻底移除自动附带当前笔记。
- src/settings.ts：新增 includeVaultIndex（默认 true）、chatContextMaxChars（默认 8000）。
- i18n en/zh：附件栏、选择器、索引设置文案。
- styles.css：附件栏/chips/选择器弹窗样式。
- 版本 0.4.0（manifest/package/versions 三文件同步）。

### 备注
- 后续用户将要求开发「加载 skill 功能」和「联网搜索功能」——两者应复用 src/context/knowledge.ts 的上下文拼接格式。
- 未提交 git（遵守死命令：未明确说 git 提交不提交）。

## Skill 功能（v0.4.0 → v0.5.0）

用户确认：全局默认 + 每会话独立；聊天框上方增「🧩 Skill」多选入口；未启用的 skill 仅列名字索引。

### 改动文件
- src/utils/aiFolder.ts：Session 加 skills: string[]（必填，迁移/创建兜底 []）。
- src/skills/skills.ts（新增）：listSkills() 递归扫描 skills/ 下 .md（排除 README/隐藏文件，取 frontmatter name>首个H1>文件名）；buildSkillContext() 产出索引块(全部名+active/inactive 标注) + 启用内容块(截断)。
- src/settings.ts：defaultSkills: string[]；设置面板「Skill 技能」分组逐文件开关全局默认；新增 import listSkills/SkillEntry。
- src/view/chatView.ts：聊天框上方 Skill 栏 + SkillPickerModal（搜索多选）；新会话继承 defaultSkills；buildSystem() 改为 async 并注入 skill 上下文。
- i18n en/zh：skill 栏、选择器、设置、索引文案。
- styles.css：skill 栏/chip/选择器路径/设置列表样式。
- 版本 0.5.0（manifest/package/versions 同步）。

### 验证
- npm run build 通过（修复：buildSystem 需 async、s.skills 类型改为必填、迁移对象补 skills）。
- 安全扫描无禁止 API；i18n 键全存在。
- 未提交 git。

### 待办（用户后续要求）
- 联网搜索功能（应复用 knowledge.ts 上下文拼接格式）。
- 手动验证（用户尚未在测试 vault 实测）。
