# 2026-08-12

## AI Note Agent 插件：知识库索引 + 显式文件/文件夹附加（v0.3.0 → v0.4.0）

按 obsidian-plugin-dev 阶段流程完成开发，已通过 tsc 构建、安全扫描、i18n 校验、版本同步。

### 需求
- AI 能看到知识库所有文件（仅路径索引，不读内容），但对话中默认不加载任何文件内容。
- 只有用户显式附加文件/文件夹，或在消息中点名某文件时，才读取其内容与上下文。
- 聊天框上方新增「附件栏 + 从知识库选择文件/文件夹」入口。

### 关键设计决策（用户确认）
- 仅显式附加：移除旧的「自动附带当前活跃笔记」逻辑（删 getNoteContext/getActiveNote）。
- 附件按会话独立保存（Session.attachments: {type:file|folder, path}[]）。
- 索引仅含 Markdown 文件。

### 改动文件
- src/utils/aiFolder.ts：Session 加 attachments 字段 + 迁移兼容。
- src/context/knowledge.ts（新增）：buildKnowledgeIndex（系统提示注入路径目录）、buildAttachmentContext（解析附件→递归读内容→拼接上下文块，单文件截断、消息点名匹配）。预留 skill/web 扩展点。
- src/view/chatView.ts：附件栏 UI + AttachmentPickerModal 选择器；发送调用附件上下文；buildSystem 注入索引；彻底移除自动附带当前笔记。
- src/settings.ts：新增 includeVaultIndex（默认 true）、chatContextMaxChars（默认 8000）。
- i18n en/zh：附件栏、选择器、索引设置文案。
- styles.css：附件栏/chips/选择器弹窗样式。
- 版本 0.4.0（manifest/package/versions 三文件同步）。

### 备注
- 后续用户将要求开发「加载 skill 功能」和「联网搜索功能」——两者应复用 src/context/knowledge.ts 的上下文拼接格式。
- 未提交 git（遵守死命令：未明确说 git 提交不提交）。

## Skill 功能（v0.4.0 → v0.5.0）

用户确认：全局默认 + 每会话独立；聊天框上方增「🧩 Skill」多选入口；未启用的 skill 仅列名字索引。

### 改动文件
- src/utils/aiFolder.ts：Session 加 skills: string[]（必填，迁移/创建兜底 []）。
- src/skills/skills.ts（新增）：listSkills() 递归扫描 skills/ 下 .md（排除 README/隐藏文件，取 frontmatter name>首个H1>文件名）；buildSkillContext() 产出索引块(全部名+active/inactive 标注) + 启用内容块(截断)。
- src/settings.ts：defaultSkills: string[]；设置面板「Skill 技能」分组逐文件开关全局默认；新增 import listSkills/SkillEntry。
- src/view/chatView.ts：聊天框上方 Skill 栏 + SkillPickerModal（搜索多选）；新会话继承 defaultSkills；buildSystem() 改为 async 并注入 skill 上下文。
- i18n en/zh：skill 栏、选择器、设置、索引文案。
- styles.css：skill 栏/chip/选择器路径/设置列表样式。
- 版本 0.5.0（manifest/package/versions 同步）。

### 验证
- npm run build 通过（修复：buildSystem 需 async、s.skills 类型改为必填、迁移对象补 skills）。
- 安全扫描无禁止 API；i18n 键全存在。
- 未提交 git。

### 待办（用户后续要求）
- 联网搜索功能（应复用 knowledge.ts 上下文拼接格式）。
- 手动验证（用户尚未在测试 vault 实测）。

## 联网搜索功能（v0.5.0 → v0.6.0）

用户确认：手动触发（聊天框上方 🌐 开关）；除 Tavily 外再加 Serper / Brave / SearXNG；多 Key 轮询、user message 注入、脚注引用全部照做。

### 改动文件
- src/search/search.ts（新增）：SearchResult 接口 + WebSearchProvider 接口；TavilyProvider / SerperProvider / BraveProvider / SearXNGProvider；WebSearchService 多凭据轮询（requestUrl，失败逐 key/实例尝试，归一化结果，单条截断）。parseKeyList 工具。
- src/search/prompt.ts（新增）：buildWebSearchContext(results, showCitations) 产出 `--- Web search results ---` 块 + [n] 引用指令，复用 knowledge.ts 区块风格。
- src/utils/aiFolder.ts：Session 加 webSearch: boolean（创建默认 false、迁移兜底 false）。
- src/settings.ts：webSearchEnabled / webSearchProvider / tavilyApiKeys / serperApiKeys / braveApiKeys / searxngInstances / webSearchMaxResults / webSearchMaxCharsPerResult / webSearchShowCitations；「联网搜索」设置分组（总开关、Provider 下拉、按 provider 显示多凭据 textarea、结果数/截断/引用开关）。
- src/view/chatView.ts：导入 WebSearchService/buildWebSearchContext；聊天框上方 🌐 开关按钮（per-session，renderWebToggle 随会话切换刷新）；handleSend 在 webSearch && 全局启用 && 有凭据时调用搜索，结果拼到当前 user message 之后；无凭据/无结果/失败给 Notice。
- i18n en/zh：联网搜索设置与提示文案。
- styles.css：.ana-chat-web-toggle（含 is-active 高亮）、.ana-settings-keys（多行等宽）。
- 版本 0.6.0（manifest/package/versions 同步）；knowledge.ts 扩展点注释更新为「已实现」。
- 未提交 git。

### 验证
- npm run build 通过；安全扫描无禁止 API；i18n 键全存在（仅 obsidian 误报）。

## UI 优化（输入区重构）

用户要求：把「添加文件/文件夹、Skill、联网搜索」三个入口改为一行图标；已选附件/Skill 显示在输入框内部；入口改为图标。

### 改动文件
- src/view/chatView.ts：
  - 移除 attachmentsEl/skillsEl 两个独立栏；新增 inputWrapEl/chipsEl/actionsEl 引用。
  - renderLayout 重构为：输入区（input-area 包裹 input-wrap + 发送按钮）+ 操作栏（actions）。
  - input-wrap 内部分为 chips + textarea；textarea 无边框，由 input-wrap 提供边框。
  - 操作栏三个图标按钮：paperclip（附件）、puzzle（skill）、globe（联网搜索），均使用 Obsidian setIcon。
  - 合并 renderAttachments/renderSkills 为 renderChips；chip 内使用 lucide 图标（folder/file-text/puzzle）替代 emoji。
  - renderActions 负责更新联网搜索图标 active 状态；切换会话时同步刷新 chips 与 actions。
- styles.css：新增/重写 ana-chat-input-area、ana-chat-input-wrap、ana-chat-chips、ana-chat-actions、ana-chat-action 样式；移除 ana-chat-attachments、ana-chat-skills、ana-chat-toolbar 等旧样式；chip 增加 ana-chat-chip-icon 槽位。
- 未新增 i18n key：复用现有 view.addAttachment / view.manageSkills / view.webToggle / view.webOn / view.webOff，通过 title 属性提供 tooltip。

## UI 优化（输入区布局二次调整）

用户要求（按截图）：图标太小要放大；发送按钮改为图标并放到输入框右下角；Skill / 联网搜索放到输入框左下角；文件入口单独放到输入框上方。

### 改动文件
- src/view/chatView.ts：
  - 移除 actionsEl；新增 attachBtn 直接放到输入框上方的 `ana-chat-attach-row`。
  - 输入框内部新增 `ana-chat-input-bar`：左下角 skillBtn（puzzle）+ webToggleBtn（globe），右下角 sendBtn（send 图标）。
  - renderActions 现在同时更新 attachBtn（有附件时高亮）、skillBtn（有 skill 时高亮）、webToggleBtn（联网搜索开关）。
- styles.css：
  - 新增 .ana-chat-attach-row（输入框上方）。
  - 重写 .ana-chat-input-wrap 为 flex 列：chips + textarea + 底部工具栏。
  - .ana-chat-action 放大到 32×32，图标 20×20。
  - .ana-chat-send 改为图标按钮（32×32，accent 背景，白色 send 图标）。
  - 移除旧的 .ana-chat-actions / .ana-chat-attach-actions。

### 验证
- npm run build 通过；i18n 键全存在（仅 obsidian 误报）。
- 后续调整：图标按钮 32→40、图标 20→24、发送按钮同步放大。
- 未提交 git。

## UI 优化（输入框细节调整）

用户要求：1. 去除点击输入框时的外边框高亮；2. 移除输入框顶部的 6px 拖拽横条，改为整个上边框区域可拖拽调整高度。

### 改动文件
- src/view/chatView.ts：
  - 移除 `resizeHandleEl` 引用与 DOM 创建。
  - `attachResizeDrag()` 改为监听 `inputWrapEl`：当鼠标在输入框顶部 6px 区域内按下时开始拖拽；鼠标移到上边框时自动显示 `ns-resize` 光标。
- styles.css：
  - 删除 `.ana-chat-input-wrap:focus-within` 规则，点击/聚焦时不再高亮外边框。
  - 删除 `.ana-chat-resize-handle` 相关样式，不再显示顶部拖拽横条。

### 验证
- `npm run build` 通过；未提交 git。

## UI 优化（输入框细节二次调整）

用户要求：1. 下边框聚焦高亮仍未去除；2. 顶部 6px 拖拽横条需要保留，否则鼠标操作不方便。

### 改动文件
- src/view/chatView.ts：恢复 `resizeHandleEl` 引用、DOM 创建与 `attachResizeDrag()` 的拖拽条监听逻辑。
- styles.css：
  - 恢复 `.ana-chat-resize-handle` 及 `::before` 样式，保留顶部 6px 可拖拽横条。
  - 在 `.ana-chat-input:focus` 中增加 `box-shadow: none`，彻底去除聚焦时的底部/内部高亮。

### 验证
- `npm run build` 通过；未提交 git。

## 重要部署坑（本轮排查发现）

**症状**：用户反复反馈「样式改了不生效」（聚焦高亮去不掉、图标调大看不出变化）。

**根因**：`styles.css` 改完只重新 `npm run build`（只重建 main.js），但 **没有把 styles.css 同步到 vault 插件目录**。Obsidian 的 `styles.css` 只在插件加载时读一次，不会热更新。

**vault 插件目录（本机）**：`D:\WorkSprace\obsidian\obsidian_vault\.obsidian\plugins\ai-note-agent\`

**正确发布流程**：`npm run build` 后，必须把 `styles.css`（以及 main.js）复制到上面这个目录，再在 Obsidian 里重新加载插件（Ctrl/Cmd+R 或 设置→社区插件 关闭/开启）才能看到样式变化。注意：源码 `D:\WorkSprace\gitee\ai-note-agent\` 与 vault 目录是两份独立文件，不是软链。

**教训**：凡是只改 styles.css 的需求，改完务必同步文件并提示用户重载插件；不要只靠 build。本轮已用 `cp` 把最新 styles.css 拷到 vault 并 `diff` 确认一致。

## UI 优化（输入框聚焦高亮 + 图标放大，本轮）

用户要求：1. 输入框聚焦仍高亮上下边框，彻底去除；2. 按钮图标太小，调大。

### 改动文件（styles.css）
- `.ana-chat-input-wrap:focus-within`：强制 `border-color: var(--background-modifier-border) !important; box-shadow: none !important; outline: none !important;`（覆盖 Obsidian 主题对聚焦边框的高亮，用 !important 提升优先级）。
- `.ana-chat-input:focus`：加 `!important`，并补 `border-color: transparent !important;` 防止 textarea 自身边框高亮。
- `.ana-chat-action` / `.ana-chat-send`：按钮 32×32 → 38×38。
- 图标：`.ana-chat-action svg, .ana-chat-action .svg-icon` 与 `.ana-chat-send svg, .ana-chat-send .svg-icon` 统一设为 `width/height: 34px !important`（原 30px，再早是 26px；直接改 svg 尺寸对 Obsidian 的 `.svg-icon` 需加 !important 才能压过默认尺寸）。

### 验证
- `npm run build` 通过。
- 已将最新 styles.css `cp` 到 vault 插件目录并 `diff` 确认一致（之前"不生效"是因为 vault 文件是旧版）。
- 未提交 git。
- 待用户重载插件后实测聚焦高亮是否彻底消失、图标是否够大。

## 设置界面分组优化（本轮）

用户要求（按截图）：优化 AI Note Agent 设置界面，加上对应分组。

### 改动文件
- src/settings.ts：
  - 新增 `createSection(containerEl, titleKey, descKey?)` 私有辅助方法，生成「分组标题 + 可选描述 + 设置项容器」三段式结构。
  - 原 display() 平铺的全部 Setting 按功能重排为 10 个分组：AI 后端 / 模型参数 / 链接与关联 / 笔记增强 / 实时自动提示 / AI 行为 / 对话记忆 / AI 对话上下文 / Skill 技能 / 联网搜索。
  - 新增 `temperature` 设置项暴露到 UI（接口已有字段但此前未显示在面板）。
  - renderDefaultSkills() 改为接收分组 body 容器（skillsBody）。
- styles.css：替换旧的 `.ana-settings-h3` 为分组样式块（`.ana-settings-section` 顶部分隔线、`.ana-settings-h3` accent 左边框、`.ana-settings-section-desc`、`.ana-settings-section-body` 首设置项去重边框）。
- src/i18n/locales/zh.json + en.json：新增分组标题/描述 key（provider/modelParams/links/enrichment/autoprompt/behavior/memory/chat/skills/web 各加 .desc）与 settings.temperature.*。

### 设计要点
- 分组沿用 Obsidian CSS 变量，无硬编码颜色；标题用 `--interactive-accent` 左边框强调。
- 仅视觉归类，不改变任何设置项的存储 key，向后兼容旧配置。

### 验证
- `npm run build` 通过（tsc + esbuild，main.js 123.2kb）。
- 安全扫描：main.js 无 createContextualFragment / new Function / eval / innerHTML。
- 生成 settings-preview.html（HTML 模拟预览，可删除）。
- 未提交 git，未同步 vault 插件目录（改了 styles.css + settings.ts，需用户重载验证）。

## 设置界面标签重命名（本轮，仍在 v0.7.0）

用户要求：将「AI 后端」分组改名为「模型配置」；分组内逐项改名：提供商下拉 "AI 后端"→"链接类型"、OpenAI API Key→API Key、OpenAI Base URL→Base URL、OpenAI 模型→模型名称；测试连接不变。

### 改动文件（仅 i18n 文案，无逻辑变更）
- src/i18n/locales/zh.json：`settings.section.provider`=模型配置；`settings.provider.name`=链接类型；`settings.openaiKey.name`=API Key；`settings.openaiBaseUrl.name`=Base URL；`settings.openaiModel.name`=模型名称。
- src/i18n/locales/en.json：对应英文 Model configuration / Link type / API key / Base URL / Model name。
- settings-preview.html：同步更新预览文案。

### 备注
- Ollama 行的 "Ollama Base URL" / "Ollama 模型" 未改（用户仅点名 OpenAI）。若需与 OpenAI 行一致，可一并改为 Base URL / 模型名称。
- `npm run build` 通过；已 cp main.js/styles.css/manifest.json 到 vault 插件目录并 diff 一致。
- 未提交 git。

## 设置界面项顺序调整（仍在 v0.7.0）

用户要求：「测试连接」放到「模型名称」后面。

### 改动文件
- src/settings.ts：将 `settings.test.name` 对应的 Setting 块从「链接类型」下拉后移除，改放到 OpenAI/Ollama 模型输入项之后（即 provider 配置全部填完再测试）。
- settings-preview.html：同步调整预览项顺序。

### 验证
- `npm run build` 通过；已同步 vault 插件目录并 diff 一致。
- 未提交 git。

## 修复聊天窗口文字无法复制（仍在 v0.7.0）

用户反馈：AI 对话框里的文字无法复制（截图中选中红框内的消息气泡无反应）。

### 改动文件
- styles.css：
  - `.ana-chat-messages` 显式加 `user-select: text; -webkit-user-select: text;`。
  - `.ana-chat-bubble` 与 `.ana-chat-text` 显式加 `user-select: text;`，防止被 Obsidian 默认/主题样式继承为 none。
- src/view/chatView.ts：
  - `attachResizeDrag()` 拖拽条在 pointerdown 时先保存 `document.body.style.userSelect` 原始值，pointerup/pointercancel 时恢复原始值（而非硬编码空字符串），避免异常情况下全局文字选择被永久禁用。
  - 增加 `pointercancel` 监听，防止拖拽中断时未清理状态。

### 验证
- `npm run build` 通过（main.js 123.3kb）。
- 安全扫描：main.js 无 createContextualFragment / new Function / eval / innerHTML。
- 已同步 main.js / styles.css / manifest.json 到 vault 插件目录并 diff 一致。
- 未提交 git。
