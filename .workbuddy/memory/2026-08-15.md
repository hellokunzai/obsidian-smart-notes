# 2026-08-15 工作日志

## 调整：模型链接弹窗最大 Token 数文案与 0 值无限制
- 需求：去除「最大 Token 数」placeholder 中的「（使用全局默认）」；设置 0 时表示无限制。
- 改动：
  - `src/modelLinkModal.ts`：placeholder 只显示全局默认值数字；保存逻辑允许 `0`。
  - `src/ai/provider.ts`：`resolveLinkParams` 接受 `ModelLink | undefined`，`maxTokens=0` 保留为无限制。
  - `src/ai/openai.ts` / `src/ai/ollama.ts`：`maxTokens=0` 时不发送 `max_tokens` / `num_predict`。
  - `src/main.ts`、`src/view/chatView.ts`、`src/optimize/optimizer.ts`：调用方使用 `resolveLinkParams`，让模型链接级参数真正生效。
  - `src/i18n/locales/zh.json` / `en.json`：更新说明文案。
- 验证：`npm run build` 通过，生成 `main.js`（153.2kb）。

## 修复：选择密钥弹窗在 Obsidian 1.13.7 列表样式崩坏
- 现象：1.12.7 正常，1.13.7 下 SecretPickerModal（选择密钥列表）样式崩。
- 根因：Obsidian 1.13 对 `.modal button` / `.modal input` 增加全局样式（specificity 0,1,1），压过插件原有单类名选择器（0,1,0），导致单选框和操作按钮被拉伸。
- 修复：用脚本把 `styles.css` 中所有 `.ana-secret-picker-*`（除容器 `.ana-secret-picker-modal` 本身）选择器统一加 `.ana-secret-picker-modal ` 父级前缀（提升到 0,2,0），并补一条兼容性补丁固化 radio/按钮的 `flex`/`margin`/`padding`。
- 验证：`npm run build` 通过，main.js 已重新生成（146.7kb）。需用户在 1.13.7 实测确认列表恢复正常。

## 修正： giant "a.." 的真正根因
- 再次看到截图后发现问题不是按钮/单选框被拉伸，而是**密钥名称文字变得巨大**。
- 根因：`styles.css` 里把 **Obsidian 的 `--font-normal`（font-weight 变量，默认 400）错误地当成 font-size 用**。当它在 font-size 上下文失效/解析异常时，span 回退继承 `.modal` 的字体大小，在 1.13.7 下被放大成巨字。
- 正确做法：UI 弹窗里的文字大小应使用 Obsidian 标准 UI 字体变量 `--font-ui-medium`（约 15px），徽章用 `--font-ui-smaller`。
- 修复：将 3 处 `font-size: var(--font-normal)` 改为 `font-size: var(--font-ui-medium)`；1 处 `font-size: var(--font-xs)`（Obsidian 无此变量）改为 `--font-ui-smaller`。已 rebuild。

## 修复：对话记录（会话持久化）完全没有落盘
- 现象：发消息时对话能正常显示，但重启 Obsidian / 重新打开插件后历史会话全部丢失。
- 根因（违反既有约定）：`src/utils/aiFolder.ts` 与 `src/memory/profileMemory.ts` 在隐藏文件夹 `.vaultmind/` 上仍使用普通 `vault` API（`getAbstractFileByPath`/`create`/`modify`/`read`/`delete`/`createFolder`）。而 Obsidian 的 vault 缓存**不索引 `.` 开头的隐藏文件夹**，导致这些操作失效；失败又被 `ChatView.persist()` 的静默 `catch` 吞掉，所以表面无报错、数据却丢了。项目 MEMORY.md 早已规定隐藏文件夹必须走 `vault.adapter`，但这两处漏改。
- 修复：将 aiFolder.ts / profileMemory.ts 全部文件 I/O 改为 `vault.adapter`（`exists`/`mkdir`/`write`/`read`/`remove`/`list`/`stat`），与 `skills.ts` 既有写法一致；并给 `persist()` 的 catch 加 `console.error`，使以后写入失败可见。
- 附带恢复：旧版可能已把 `session-*.json` 实际写到磁盘但索引缺失/无法读回；新增 `recoverFromOrphanedFiles()`，在 index 为空时扫描 sessions 目录重建索引，救回残留历史对话。
- 验证：`npm run build` 通过（0.11.1，main.js 147.0kb），安全扫描 0 命中禁用 API。需在真实 vault 实测：发多条消息→关闭重开→历史仍在。

## 优化：AI 对话窗口体验升级（0.11.1 → 0.12.0）
- Markdown 渲染：助手消息使用 `MarkdownRenderer.renderMarkdown` 渲染，支持表格、代码块、列表、引用等。
- 流式输出：`AIProvider` 新增 `stream()`，OpenAI / Ollama 分别实现 SSE / NDJSON 流式解析；聊天视图按 token 增量更新并带闪烁光标动画。
- 错误内联提示：联网搜索失败、请求超时、连接不上等统一在对话框内以错误气泡展示，不再弹 Notice。
- Token 消耗提示：助手消息底部显示 prompt/completion/total；接口未返回时按字符粗略估算并标注“约”。
- 数据模型：`SessionMessage` 增加可选 `usage` 字段，历史消息加载时也能显示 token 消耗。
- 样式：`styles.css` 新增 Markdown 内容样式、流式光标动画、消息出现动画、token 消耗 footer 样式。
- 验证：`npm run build` 通过（0.12.0，main.js 152.9kb），安全扫描 0 命中禁用 API。

## 样式调整：代码块复制按钮移至右上角
- 需求：用户截图显示复制按钮在代码块右下角且有背景，希望移到右上角、图标缩小、背景透明、但保留交互范围。
- 修改：`styles.css` 中给 `.ana-chat-message-assistant .ana-chat-text pre` 加 `position: relative`，并覆盖 `.copy-code-button`：绝对定位 `top/right: 0.35em`、`background: transparent`、无阴影边框、`transform: scale(0.8)`、`opacity: 0.55`（hover 1），padding 保持 0.4em 以保留可点击区域。
- 验证：`npm run build` 通过，main.js 152.9kb。

## 调整：模型链接弹窗输入框改为数字输入框并设默认值
- 需求：最大 Token 数 / Temperature 输入框改为只能填数字；新建链接时默认最大 Token 数=0、Temperature=0.3。
- 改动：
  - `src/modelLinkModal.ts`：两输入框分别设置 `type="number"` 及 `min/step/max`；新建时 `maxTokens` 初始化为 `"0"`，`temperature` 初始化为 `"0.3"`；编辑时仍使用已保存值；去除 temperature placeholder 中的「（使用全局默认）」。
  - `src/i18n/locales/zh.json` / `en.json`：删除不再使用的 `settings.modelLinks.modal.useGlobalDefault` 键。
- 验证：`npm run build` 通过，生成 `main.js`（153.3kb）。

## 调整：去除全局默认文案并统一数字输入框宽度
- 需求：去除「最大 Token 数」/「Temperature」描述中的「留空则使用全局默认值」；让两个数字输入框宽度一致。
- 改动：
  - `src/i18n/locales/zh.json` / `en.json`：更新 `maxTokensDesc` / `temperatureDesc`，删除「留空则使用全局默认值」。
  - `styles.css`：为 `.ana-model-link-modal .setting-item-control input[type="number"]` 统一设置 `width: 90px`，避免描述长度不同导致输入框宽度不一致。
- 验证：`npm run build` 通过，生成 `main.js`（153.1kb）。

## 修正：数字输入框宽度改为对齐 Base URL 输入框
- 反馈：`width: 90px` 把两个数字输入框压得过窄，用户要的是和 Base URL 文本输入框长度一致（不是调短）。
- 改动：`styles.css` 将 `.ana-model-link-modal .setting-item-control input[type="number"]` 改为 `width:100%` + `flex:1 1 auto` + `min-width:0` + 左对齐，撑满控制区，与 Base URL 等文本输入框同宽。
- 验证：`npm run build` 通过，生成 `main.js`（153.1kb）。

## 再修正：提升 CSS 优先级确保数字输入框宽度生效
- 反馈：截图显示数字输入框仍保持旧样式的窄宽度且右对齐，说明 `styles.css` 未 reload。
- 改动：
  - `styles.css` 选择器加 `.modal` 前缀提升 specificity，并对 width/min-width/max-width/flex/text-align 使用 `!important`，确保覆盖 Obsidian 默认样式。
  - 添加注释提醒修改 styles.css 后需要在 Obsidian 中 reload 插件或按 Ctrl/Cmd+R。
- 验证：`npm run build` 通过，生成 `main.js`（153.1kb）。

## 修复：移动端模型链接弹窗「选择秘钥/测试」按钮一行显示
- 需求：用户在移动端截图反馈「选择秘钥」和「测试」两个按钮上下堆叠，希望改成一行并排。
- 根因：`styles.css` 移动端媒体查询中 `.ana-model-link-key-btn-row` 仅设置 `flex-wrap: wrap`，且按钮未强制宽度；Obsidian 移动端 `.modal button` 全局样式可能把按钮拉成整行宽度，导致换行堆叠。
- 改动：`styles.css` 中把移动端 `.ana-model-link-key-btn-row` 选择器加 `.ana-model-link-modal` 父级前缀提升 specificity，设置 `flex-wrap: nowrap; width: 100%;`，并把内部按钮设为 `flex: 1 1 0; min-width: 0; width: auto;` 以均分一行宽度。
- 验证：`npm run build` 通过，生成 `main.js`（154.7kb）。

## 修复：移动端设置页「联网搜索」密钥按钮一行显示
- 需求：用户截图显示设置页「Tavily API Key」里的「修改秘钥」和「测试」按钮同样在移动端上下堆叠，要求一并改成一行。
- 根因：设置面板（`.ana-settings-panel-body`）也复用了 `.ana-model-link-key-btn-row`，但上一条修复只加了 `.ana-model-link-modal` 前缀，未覆盖设置面板。
- 改动：`styles.css` 中把移动端按钮行规则的选择器扩展为 `.ana-model-link-modal .ana-model-link-key-btn-row, .ana-settings-panel-body .ana-model-link-key-btn-row`（按钮同步扩展），使弹窗和设置面板共用同一套强制并排样式。
- 验证：`npm run build` 通过，生成 `main.js`（154.7kb）。

## 再修正：改用 inline style 控制数字输入框宽度
- 反馈：即使加了 `!important`，截图显示数字输入框仍保持窄宽度且右对齐，CSS 方式始终无法生效。
- 改动：在 `src/modelLinkModal.ts` 中，对「最大 Token 数」和「Temperature」的 `TextComponent.inputEl` 直接设置 inline style：`width:100%`、`maxWidth:none`、`textAlign:left`；并对对应的 `Setting.controlEl` 设置 `flex:1 1 auto`、`minWidth:0`，确保控制区撑满。绕开 `styles.css` 加载/缓存问题。
- 验证：`npm run build` 通过，生成 `main.js`（153.4kb）。

## 再修正：inline style 也加 `!important`，并约束 label 区不抢占空间
- 反馈：截图仍显示数字输入框窄宽度且右对齐，疑似旧 `main.js` 未 reload。
- 改动：
  - `src/modelLinkModal.ts` 中将所有 inline style 改为 `setProperty(..., "important")`，确保覆盖任何 Obsidian 默认/主题样式。
  - 增加 `inputEl.style.minWidth = "200px"`（important），防止输入框被压到Intrinsic窄宽度。
  - 增加 `infoEl.style.flex = "0 1 auto"`（important），防止标签描述区把控制区挤窄。
- 验证：`npm run build` 通过，生成 `main.js`（153.9kb）。

## 最终修正：数字输入框宽度对齐「链接名称」输入框
- 反馈：用户明确表示要与「链接名称」输入框长度一致，而非 Base URL；之前 `!important` + `min-width:200px` + 控制区 flex 覆盖导致布局变形。
- 改动：
  - `src/modelLinkModal.ts`：移除所有 `!important`、控制区 flex 覆盖与 `min-width`；仅保留 `inputEl.style.width = "100%"` 和 `textAlign = "left"`，让数字输入框像普通文本输入框一样撑满控制区。
  - `styles.css`：删除之前为数字输入框写的覆盖规则。
- 验证：`npm run build` 通过，生成 `main.js`（153.2kb）。

## 补充：为数字输入框宽度加 `!important`
- 反馈：用户截图确认对齐效果，为进一步确保与「链接名称」输入框宽度一致，将 `width: 100%` 升级为 `setProperty("width", "100%", "important")`，强制覆盖浏览器/主题对 `input[type="number"]` 的默认宽度。
- 改动：`src/modelLinkModal.ts` 中两处数字输入框的 `style.width` 改为 `setProperty("width", "100%", "important")`。
- 验证：`npm run build` 通过，生成 `main.js`（153.2kb）。

## 最终方案：运行时自动测量「链接名称」输入框宽度并同步
- 反馈：用户截图指出数字输入框因 `width: 100%` 撑满控制区，比「链接名称」输入框长出一截。
- 改动：
  - `src/modelLinkModal.ts`：保存「链接名称」「最大 Token 数」「Temperature」三个输入框的引用；在弹窗布局完成后通过 `requestAnimationFrame` 读取「链接名称」输入框的 `offsetWidth`，并将两个数字输入框的 `max-width` 同步设为该像素值，确保与「链接名称」完全等宽。
  - 保留 `width: 100% !important` 与 `text-align: left`，防止浏览器默认窄宽度与右对齐。
- 验证：`npm run build` 通过，生成 `main.js`（153.5kb）。

## 设置页数值输入框改为数字输入框
- 需求：将交互设置/记忆与角色/联网搜索中的 4 个数值设置项输入框改为数字输入框，且不改变输入框长度。
- 改动：`src/settings.ts` 中以下 4 处 `.addText()` 改为设置 `inputEl.type = "number"`、`min="1"`、`step="1"`、`inputMode="numeric"`，未添加任何宽度样式：
  - `realtimeDebounceMs`（实时防抖）
  - `chatContextMaxChars`（单文件注入字符上限）
  - `webSearchMaxResults`（最大结果数）
  - `webSearchMaxCharsPerResult`（单条结果字符上限）
- 验证：`npm run build` 通过，生成 `main.js`（153.9kb）。

## 修复：AI 对话窗口文件链接点击无法跳转（0.12.0 → 0.12.1）
- 现象：AI 输出的 `[文本](app://obsidian.md/...)` 链接点击后无反应，无法跳转到对应笔记。
- 根因：
  - 对话 system prompt 未明确约束链接格式，AI 可能输出 `app://obsidian.md/` 协议链接；
  - 自定义视图用 `MarkdownRenderer.renderMarkdown` 渲染后，Obsidian 不会自动处理这些 `<a>` 的点击行为。
- 修复：
  - `src/view/chatView.ts`：为 `messagesEl` 添加委托点击事件，拦截 `<a>`；对 `app://obsidian.md/<路径>` 提取并解码 vault 路径，对相对 `.md` 链接直接取路径，然后调用 `workspace.getLeaf(false).openFile()` 打开；外部 `http(s)://` 与 `obsidian://` 链接保持默认行为。
  - `src/view/chatView.ts`：在 `buildSystem()` 的 base prompt 中追加约束——要求 AI 使用标准 Markdown 链接 `[text](path/to/note.md)`，禁止 `[[...]]` 与 `app://obsidian.md/`。
  - `src/i18n/locales/zh.json` / `en.json`：新增 `view.fileNotFound` 翻译键。
- 版本：patch 升级至 `0.12.1`（manifest.json / package.json / versions.json 三文件同步）。
- 验证：`npm run build` 通过（main.js 154.7kb），安全扫描无禁用 API；已将 `main.js`、`styles.css`、`manifest.json` 复制到 vault 插件目录，需用户在 Obsidian 中 reload 插件后测试点击链接。
