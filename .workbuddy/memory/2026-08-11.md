# 2026-08-11

## ai-note-agent 插件：新增「自定义指令 + 对话记忆 + AI 文件夹」

在 Obsidian AI 插件中按 obsidian-plugin-dev skill 流程（阶段一→二→四）完成增量开发：

- **自定义指令**：`settings.ts` 新增 `customInstructions` 字段与多行文本设置项；`ai/prompt.ts` 新增 `buildSystemPrompt()` 把指令注入所有 AI 功能的 system prompt。
- **对话记忆**：`chatView.ts` 改为维护 `conversationHistory` 实现多轮对话，复盘后落盘；新增开关 `enableMemory` 与上限 `maxMemoryMessages`。
- **AI 文件夹**：新建 `src/utils/aiFolder.ts`，`onload` 时在 vault 根目录（默认 `AI-Note-Agent`，可配置）自动生成 `memory/chat-history.json` 与 `skills/` 目录；记忆加载/保存/清空逻辑全部走 Obsidian Vault API。
- i18n 补全 en+zh；版本号 0.1.0 → 0.2.0（manifest/package/versions 同步）；`npm run build` 通过，安全扫描无 `eval/new Function/createContextualFragment`。

**注意**：按用户死命令，未执行任何 git 提交。
